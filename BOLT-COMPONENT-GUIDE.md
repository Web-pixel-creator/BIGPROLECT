# 🎨 Руководство по использованию Premium UI компонентов в Bolt.diy

## Что изменилось?

Система Bolt.diy теперь имеет доступ к **582 премиум компонентам** из топовых библиотек:

- ✨ **Aceternity UI** - анимированные компоненты
- 🪄 **Magic UI** - эффекты и анимации
- 🥥 **Kokonut UI** - современная библиотека
- 🎭 **shadcn/ui blocks** - готовые блоки
- ⚛️ **React Bits** - паттерны React

## Как это работает?

### 1. Система автоматически загружает компоненты

Когда вы пишете промпт с ключевыми словами, система:
- Находит подходящие компоненты в MD файлах
- Добавляет их код в контекст модели (до 8 компонентов, 5KB каждый)
- Модель получает явные инструкции использовать эти компоненты

### 2. Ключевые слова для активации компонентов

**Эффекты:**
- `sparkles` / `звёзды` → Sparkles компонент
- `aurora` / `аврора` → Aurora Background
- `gradient` / `градиент` → Gradient Text/Background
- `glow` / `свечение` → Glow эффекты
- `particles` / `частицы` → Particle эффекты
- `animated cursor` → Анимированный курсор
- `plasma` / `плазма` → Plasma Background
- `meteor` / `метеор` → Meteor эффект
- `grid` / `сетка` → Grid Background
- `waves` / `волны` → Wave эффекты

**Компоненты:**
- `hero` / `hero секция` → Hero компоненты
- `card` / `карточка` → Card с эффектами
- `button` / `кнопка` → Анимированные кнопки
- `navbar` / `навигация` → Navbar компоненты
- `pricing` / `цены` → Pricing компоненты
- `testimonials` / `отзывы` → Testimonial компоненты

## Примеры промптов

### ✅ Хорошие промпты (используют компоненты):

```
Создай лендинг с эффектом sparkles и aurora фоном
```

```
Сделай hero секцию с gradient text и glowing stars
```

```
Добавь animated cursor и plasma background
```

```
Создай pricing секцию с gradient borders и hover effects
```

### ❌ Плохие промпты (слишком общие):

```
Создай лендинг
```

```
Сделай красивый дизайн
```

## Решение проблем

### Проблема 1: Создаётся только Hero секция

**Причина:** Модель не понимает, что нужна полная структура страницы.

**Решение:** Используйте более конкретный промпт:

```
Создай ПОЛНЫЙ лендинг с:
- Hero секцией с aurora background и sparkles
- Features секцией с 6 карточками
- Pricing секцией с 3 тарифами
- CTA секцией
- Footer
```

### Проблема 2: Анимации лагают

**Причина:** Слишком много частиц или неоптимизированный код.

**Решение:** Система теперь включает инструкции по оптимизации:
- Ограничение частиц до 50-100
- Использование `requestAnimationFrame`
- GPU-ускоренные трансформации (`translate3d`)
- Debounce для событий
- `will-change` CSS свойство

Попросите модель:
```
Оптимизируй анимации - уменьши количество частиц до 50 и используй requestAnimationFrame
```

### Проблема 3: Компоненты не используются

**Причина:** Промпт не содержит ключевых слов.

**Решение:** Добавьте конкретные эффекты в промпт:
```
Создай hero с aurora background, sparkles, gradient text и glow buttons
```

## Структура полного лендинга

Модель теперь знает, что полный лендинг должен включать:

1. **Navigation** - фиксированная навигация с blur эффектом
2. **Hero Section** - главная секция с эффектами
3. **Features Section** - 3-6 карточек функций
4. **How It Works** - пошаговое объяснение (опционально)
5. **Testimonials** - отзывы клиентов (опционально)
6. **Pricing** - тарифы (для SaaS)
7. **CTA Section** - финальный призыв к действию
8. **Footer** - футер с ссылками

## Оптимизация производительности

Система теперь автоматически применяет:

### 1. Ограничение частиц
```tsx
const PARTICLE_COUNT = 50; // Не 1000!
```

### 2. GPU-ускорение
```tsx
<div style={{ transform: `translate3d(${x}px, ${y}px, 0)` }} />
```

### 3. requestAnimationFrame
```tsx
useEffect(() => {
  let animationId: number;
  const animate = () => {
    // Анимация
    animationId = requestAnimationFrame(animate);
  };
  animationId = requestAnimationFrame(animate);
  return () => cancelAnimationFrame(animationId);
}, []);
```

### 4. Intersection Observer
```tsx
// Анимация только когда элемент виден
const [isVisible, setIsVisible] = useState(false);
const ref = useRef(null);

useEffect(() => {
  const observer = new IntersectionObserver(([entry]) => {
    setIsVisible(entry.isIntersecting);
  });
  if (ref.current) observer.observe(ref.current);
  return () => observer.disconnect();
}, []);
```

## Тестирование

После изменений:

1. **Перезапустите bolt.diy:**
   ```bash
   pnpm run dev
   ```

2. **Попробуйте промпт:**
   ```
   Создай полный SaaS лендинг с:
   - Hero секцией с aurora background и sparkles
   - Features секцией с 6 анимированными карточками
   - Pricing секцией с gradient borders
   - CTA секцией с glow button
   - Footer
   
   Оптимизируй анимации для плавной работы
   ```

3. **Проверьте:**
   - ✅ Все секции созданы (не только Hero)
   - ✅ Используются компоненты из контекста
   - ✅ Анимации работают плавно (60 FPS)
   - ✅ Код оптимизирован

## Что дальше?

Если проблемы остаются:

1. **Проверьте логи** - убедитесь что компоненты загружаются:
   ```
   INFO   component-matcher  Total loaded: 582 components in 6 categories
   INFO   stream-text  Added matching UI components to prompt context
   ```

2. **Увеличьте лимиты** - если нужно больше компонентов:
   - Откройте `Projects/bolt.diy/app/lib/services/componentMatcher.ts`
   - Измените `MAX_COMPONENTS` с 8 на 12
   - Измените `MAX_CODE_LENGTH` с 5000 на 8000

3. **Добавьте свои компоненты** - создайте новый MD файл в корне проекта

## Полезные ссылки

- [Aceternity UI](https://ui.aceternity.com)
- [Magic UI](https://magicui.design)
- [Kokonut UI](https://kokonutui.com)
- [shadcn/ui](https://ui.shadcn.com)

---

**Версия:** 1.0.0  
**Дата:** 2 декабря 2024
